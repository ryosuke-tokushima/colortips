<div class="container mb-5 pt-2">
  <div class="row">
    <div class="col-md-12 mb-3">
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <table class="table table-striped">
        <thead>
        <tr>
          <th scope="col"><%= Post.human_attribute_name(:id) %></th>
          <th scope="col"><%= Post.human_attribute_name(:user_id) %></th>
          <th scope="col"><%= Post.human_attribute_name(:body) %></th>
          <th scope="col"><%= Post.human_attribute_name(:created_at) %></th>
        </tr>
        </thead>
        <tbody>
        <%= render @posts %>
        </tbody>
      </table>
    </div>
  </div>
</div>
